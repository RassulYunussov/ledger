package config

const (
	major               = "0"
	minor               = "0"
	patch               = "1"
	commit              = "snapshot"
	APPLICATION_VERSION = major + "." + minor + "." + patch + "-" + commit
)
